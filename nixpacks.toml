[phases.setup]
nixPkgs = ['nodejs_20', 'openssl']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'npx prisma generate',
  'npm run build'
]

[phases.deploy]
cmds = ['npx prisma migrate deploy']

[start]
cmd = 'npm run start:prod'
